import Navigation from '../Navigation/Navigation';
import css from './AppBar.module.css';

function AppBar() {
  return (
    <div >
      <header>
       <Navigation/>
      </header>
    </div>
  );
}

export default AppBar;